import "../core/schemas/Schema/Schema.pkl"
import "../core/schemas/ConfigSchema/ConfigSchema.pkl"
import "../core/generation/Library.pkl"

config = new ConfigSchema.Config {
  bike = new Schema.BikeConfiguration {
    bikes = new Mapping<String, Schema.Bike> {
      [Library.library.bike.types.paid.awesome] = new Schema.Bike {
        name = "Hello"
        description = new Listing<String> {
          "Hey"
        }
      }
    }
  }
}

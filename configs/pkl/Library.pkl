module pkl.generation.Library

import "../schemas/Schema.pkl"

library { 
	bike { 
		types { 
			awesome = "awesome"
			cheap = "cheap"
			custom = "custom"
			expensive = "expensive"
			rusty = "rusty"
			specialized = "specialized"
		}
	}
}


module pkl.generation.Library

import "../pkl.generation.Library"

library { 
	bike { 
		types { 
			awesome = "awesome"
			cheap = "cheap"
			custom = "custom"
			expensive = "expensive"
			rusty = "rusty"
			specialized = "specialized"
		}
	}
}

